# 使用者指南——你和工具的完整互动流程

這份指南只講「你」要做什麼、會收到什麼。系統內部怎麼運作不用你管。

---

## 階段 0：一次性安裝（現在，跟 ZCode 一起完成）

1. **你**：看完設計文檔 → 回覆 "proceed" + 選 droplet IP
2. **ZCode**：實施全部六步（回測/信號賬本/Serenity/宏觀層/絆網/部署）並跑驗收
3. **你（唯一要動手的部分，約 10 分鐘）**：
   - 手機裝 Telegram，找 @BotFather 建一個 bot 拿到 token
   - 找 @userinfobot 拿到你的 chat_id
   - SSH 到 droplet，把兩個值填進 `/opt/stock-signal/.env`（想加 Astra 分析就同時填 OpenAI key）
   - 收到第一條測試推送 = 裝完
4. **ZCode**：把 Webull 上查到的 T-REX 反向 CRWV 代碼給我（或自己加進 watchlist.txt，標 leveraged_inverse）

之後的互動分成：日常（零互動）、每週（2 分鐘）、改動時（低頻）、出問題時、每季（決策）。

---

## 階段 1：日常——絕大多數日子你什麼都不用做

### 一個典型交易日的時間線（香港時間，11 月初起順延一小時）

| 時間 | 系統在做什麼 | 你會收到什麼 |
|---|---|---|
| 全天 | 絆網每 2-3 分鐘靜默掃 SPY/QQQ/VIX/期貨 + 關鍵詞新聞 | 沒事就靜默 |
| 約 21:30–04:00（美股時段） | 你的股票每 15 分鐘掃描；絆網持續戒備 | 只在觸發時響（見下） |
| 約 04:00–05:00（收盤後） | 自動生成日報並推送 | 📊 每日報告 ×1 |
| 你醒來 | — | 打開 Telegram，1 分鐘讀完日報 |

### 手機會響的四種情況（沒有其他情況會打擾你）

1. ⚡ **即時事件警報**：指數 5 分鐘內 ±1% / VIX +10% / 期貨隔夜 ±1% / 關鍵詞新聞命中
   ——格式：「⚡ SPY -1.2%｜VIX +14%｜[路透] 伊朗襲擊油輪」
2. ▲ **盤中異動**：個股 ±3%（槓桿產品 ±6%）
3. 🛑 **止損提醒**：槓桿產品盤中 -5%
4. 📊 **每日報告**：收盤後一次（含宏觀面、消息面、財報/經濟日曆倒數、Serenity 動態）

### 聽到警報後你的動作（唯一的「交易」環節）

收到警報 → 打開 Webull → **你自己判斷、自己按鈕**。
系統永遠只提醒、不下單；不動作也是一個決定。

---

## 階段 2：每週（2 分鐘，可選）

- 週日自動推送 **記分卡**：每條規則的命中率排行榜、本週觸發摘要
- 你掃一眼就知道：哪些警報值得更認真對待、哪些快變成噪音了

## 階段 3：想改動時（任何時候）

| 想改什麼 | 怎麼做 |
|---|---|
| 加/刪監控股票 | SSH 編輯 `/opt/stock-signal/watchlist.txt`（或直接叫 ZCode），存檔即生效 |
| 加新聞關鍵詞（如新總統名字） | 編輯關鍵詞檔案，或叫 ZCode |
| 加經濟日曆事件 | 編輯 `macro_calendar.txt` |
| 調閾值（異動 %、止損 %） | 編輯 `.env`，下次掃描生效 |

## 階段 4：出問題時

| 症狀 | 處理 |
|---|---|
| 美股開盤日卻一整天沒任何推送 | 叫 ZCode 檢查，或自己跑 `/opt/stock-signal/run.sh --check` |
| Telegram 正常但想確認系統活著 | 同上，--check 會顯示各數據源狀態 |
| droplet 故障 | ZCode 恢復；急用可啟用 Mac 備用定時任務（一條命令） |
| 想徹底停用 | 3 條命令（見 README），或叫 ZCode |

## 階段 5：每月/每季（數據驅動的決策點）

- **月度**：ZCode 出彙總——警報命中率、絆網延遲 vs 價格走勢、Serenity 提股後表現
- **由此決定**（不拍腦袋）：要不要調閾值、Serenity 層留不留、要不要付費升級新聞速度（如 Benzinga）
- **季度**：更新經濟日曆裡的 FOMC/CPI 日期（ZCode 提醒你）

---

## 你永遠不需要做的事

- 盯盤（系統替你盯）
- 寫代碼或碰任何技術細節（要改就開口）
- 把任何帳號密碼/API key 給任何人（包括 ZCode）——key 只放 .env
- 在情緒上頭的瞬間倉促決策——警報會再來，紀律不會
